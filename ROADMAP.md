# 全书路线图

> 最近更新：2026-07-30

这份路线图记录《终端与 AI 编程手册》从 V1.0 到 V2.0 的改造与发布过程。V2.0 已完成主体重写、技术终审，并通过 Pull Request #2 以 squash 方式合并到私有源仓库 `main`。下一阶段是将经过审查的 V2.0 内容升级到现有公共仓库，同时保留 V1.0 的提交历史。

## 1. V2.0 为什么重写

V1.0 已覆盖终端、Git、SSH、Python、Docker、Claude Code、Codex CLI、Grok Build 和 GPU 远程开发，但部分章节仍像知识卡片：段落较碎，命令之间缺少完整场景，一些配置与版本事实也变化很快。

V2.0 的目标不是单纯增加篇幅，而是把全书改造成能够连续阅读和实际操作的教程。每章尽量交代：

- 命令应该在哪台机器、哪个目录或哪个界面执行；
- 为什么现在需要执行；
- 代表性输出是什么；
- 怎样判断成功；
- 出错时先检查哪一层；
- 哪些操作会删除、覆盖、上传、扩大权限或影响远程机器；
- 怎样预演、停止和恢复。

## 2. V2.0 的五条主线

### 终端与文件操作

读者从 Terminal、Shell、zsh、目录、路径和快捷键开始，在独立练习目录中完成导航、文件操作、搜索、日志、管道和进程控制。重点不是背命令，而是建立确认机器、用户和目录的习惯。

### 开发基础设施

Git、SSH、Homebrew、Python 和 Docker 围绕真实任务组织。章节不仅解释概念，还安排分支与恢复、远程连接、依赖重建、Compose 多服务项目、Volume 数据和 GPU 容器等连续练习。

### AI 编程客户端

Claude Code、Codex CLI 和 Grok Build 分别说明安装、登录、权限、Sandbox、会话、非交互运行、Worktree 和扩展能力。工具差异与通用工作流分开讲，避免把某个客户端的按钮写成普遍规律。

### 配置、凭证、供应商与多实例

Part 10C 是 V2.0 新增核心模块，覆盖：

- Claude Code 的 Settings、环境变量、官方认证、API 和 Gateway；
- Codex 的 `config.toml`、`auth.json`、Profile、Provider、Keychain 与 `CODEX_HOME`；
- 模型、供应商、账号、Profile 和独立实例的区别；
- CC Switch、ccswitch 与 Cockpit Tools 的配置投影、凭证替换和本地路由；
- 切换前后的备份、观察、恢复和 401/403/404 排查；
- 第三方工具的数据链、凭据接触面与服务条款边界。

### AI 工作流与远程 GPU

Part 11 与 Part 12 以 Mac 日常开发、Ubuntu 24.04 游戏本提供 NVIDIA GPU 为默认场景，完整连接 SSH、rsync、Tailscale、Python 环境、CUDA、tmux、日志、checkpoint、VS Code Remote SSH 和 AI CLI。

通用工作流统一为：

```text
定义完成标准
→ 建立现实基线
→ 只读调查
→ 选择方案
→ 小批实施
→ 证据验证
→ 独立复核与人工提交
```

## 3. 当前仓库状态

```text
私有源仓库：anyue-git/Terminal-AI-Coding-Handbook
默认分支：main
V2.0 重写 PR：#2
PR 状态：已合并
合并方式：squash
合并提交：abe389994f1841c90f35c670b16420f18859c9ba
```

V2.0 的开发分支和 85 个中间提交没有直接进入 `main` 历史；squash merge 将它们整理为一个合并提交。私有源仓库继续用于后续维护，公开仓库用于对外发布。

## 4. 实施阶段

### 第一阶段：建立标准与样章——已完成

- 更新写作与内容审查规范；
- 重写 README 和阅读路线；
- 建立连续练习、输出解释和恢复方法标准；
- 新增配置、凭证、多供应商和多实例模块；
- 建立 Draft PR 与严格 Markdown 检查。

### 第二阶段：重写新手主路径——已完成

- 快速开始与完整 Quickstart；
- Part 01 基础篇；
- Part 02 终端命令；
- Part 03 Shell 快捷键；
- Part 04 Git。

### 第三阶段：重写开发基础设施——已完成

- Part 05 SSH；
- Part 06 Homebrew；
- Part 07 Python 环境；
- Part 08 Docker。

### 第四阶段：重写 AI CLI 与配置体系——已完成

- Part 09 Claude Code；
- Part 10 Codex CLI；
- Part 10B Grok Build；
- Part 10C 配置、凭证与多实例。

### 第五阶段：GPU、工作流和附录——已完成

- Part 11 GPU 远程开发全部 8 篇；
- Part 12 AI 开发工作流全部 7 篇；
- 快捷键、危险命令、术语和版本化工具附录；
- 内容审查记录与更新记录；
- README、SUMMARY 和交叉链接同步。

### 第六阶段：发布前技术终审——已完成

- 扫描并移除正文中的作者用户名和个人路径示例；
- 未发现真实 GitHub Token、API Key 或 Bearer 凭据特征；
- 将 Markdown 中依赖 GNU `find -maxdepth` 的示例改为 macOS 与 Ubuntu 均可运行的形式；
- 复核危险命令只出现在风险说明、反例或明确受控场景中；
- 确认 PR 不存在未解决的内联评审线程；
- Markdown Check #176 成功完成。

### 第七阶段：合并与公共升级——进行中

- PR #2 已从 Draft 切换为 Ready；
- 已通过 squash merge 合入私有源仓库 `main`；
- 正在修正合并后的状态文字并准备 V2.0 发布说明；
- 计划将现有公共 V1.0 仓库升级为 V2.0，同时保留原有提交历史；
- 不重写公共仓库历史，不覆盖或删除 V1.0 提交记录。

## 5. 技术终审结论

### 技术事实最新性

变化较快的内容包括 Claude Code、Codex CLI、Grok Build、Docker Compose、uv、Tailscale、PyTorch、NVIDIA 与第三方 Provider。相关章节按 2026-07-30 的官方文档和版本化原则完成文档审查，但软件后续仍可能变化。正文保留“以当前帮助和官方文档为准”的边界，不把快速变化的行为写成永久保证。

### 真实环境抽样

真实设备抽样属于发布后的持续维护项，不再作为合并当前文档重写的阻断条件。无法实际执行的内容保留为“文档核对”或“概念示例”，不宣称已经在所有设备上验证。

### 敏感信息扫描

已检查真实用户名和邮箱、公网与内网地址、API Key、Token、SSH 私钥或指纹、Cookie、Session、Refresh Token、内部服务地址、Tailscale 登录链接以及真实数据路径。正文中的示例用户名已统一匿名化。仓库所有者 `anyue-git` 属于公开仓库元数据，不作为正文个人信息残留处理。

### 教学体验审查

主体章节已完成跨章通读和结构统一。后续发现的文风、版本或工具行为问题作为常规维护修订处理，不再阻断 V2.0 发布。

## 6. 写作验收标准

V2.0 章节应满足：

- 正文以完整自然段为主，不连续堆叠只有一句话的段落；
- 新概念至少有一个具体场景，核心章节提供连续练习；
- 重要命令说明机器、目录、影响对象和成功验证；
- 常见报错说明问题层级、检查顺序和恢复方法；
- 列表只用于真正并列的信息；
- 配置和凭证章节区分可提交设置、个人配置和秘密；
- 高风险操作紧邻预演、备份、验证和恢复说明；
- 版本化内容标注核对日期，并以官方文档和本机帮助为准；
- Markdown 严格检查保持 0 个错误、0 个警告。

## 7. 公共发布方式

现有公共 V1.0 仓库将升级为通用公共仓库，并保留原来的 V1.0 历史。推荐发布顺序：

```text
私有源仓库完成终审并合并
→ 修正合并后的状态文字
→ 生成 V2.0 发布说明
→ 将 V2.0 内容作为正常提交同步到现有公共仓库
→ 运行 Markdown 检查
→ 抽样打开关键章节
→ 创建 V2.0 标签与 GitHub Release
```

许可证属于作者的独立发布决定。在作者明确选择前，不自动添加任何许可证文件；这不会阻止代码和文档公开展示，但会影响他人复制、修改和再分发的明确授权范围。

当前状态：V2.0 已合并到私有源仓库 `main`，正在准备升级现有公共仓库。