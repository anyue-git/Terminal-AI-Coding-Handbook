# 全书路线图

这份路线图用于控制《终端与 AI 编程手册》的范围和完成状态，避免项目持续扩写后变成互不关联的笔记集合。

当前原则：

```text
先完成真实工作流
→ 再统一审查和压缩
→ 最后准备干净的公开版本
```

当前私有仓库继续承担开发、核对和版本记录。未来公开时计划新建仓库，只导入经过审查的最终文件，不携带私有开发历史。

---

## 当前内容结构

### 快速入口

已完成：

- 终端十五分钟上手；
- AI CLI 快速上手；
- 终端与 AI CLI 完整快速入门；
- 快捷键速查表；
- 危险命令清单。

三个入口分别承担：

```text
终端急救
AI CLI 安全启动
第一次完整练习
```

### Part 01–03：终端基础

已覆盖：

- Terminal、Shell 和 zsh；
- 文件系统、目录和路径；
- 文件创建、复制、移动和删除；
- 文本查看、日志和搜索；
- 管道、重定向和进程；
- 命令行编辑快捷键。

### Part 04–08：开发基础设施

已覆盖：

- Git 工作区、提交、分支、恢复和 Pull Request；
- SSH、密钥、Config、rsync、端口转发和排错；
- Homebrew、PATH、版本和服务；
- Python 解释器、pip、venv、Conda、uv 和锁文件；
- Docker、Compose、Volume、网络和 GPU 容器。

### Part 09–10B：AI 编程 CLI

已覆盖：

- Claude Code；
- Codex CLI；
- Grok CLI；
- 文件、命令、网络、Sandbox 和审批；
- 会话、记忆、Hooks、MCP 和扩展；
- 交互与非交互自动化；
- Git 协作和多阶段任务；
- DeepSeek、LLM Gateway 和第三方供应商接入。

当前优先维护 Claude Code、Codex CLI 和 Grok CLI，不主动扩展到所有社区 Agent。

### Part 11：GPU 远程开发

已覆盖：

- Mac 与 Ubuntu 游戏本局域网部署；
- Tailscale 异网连接；
- Git 与 rsync 同步；
- tmux；
- NVIDIA 驱动、CUDA 和 PyTorch；
- Mac 与 Ubuntu 分别创建环境；
- VS Code Remote SSH；
- GPU 容器；
- 实验目录、日志和 checkpoint；
- 从 Mac 到 Ubuntu GPU 的端到端案例。

### Part 12：AI 开发工作流

已覆盖：

- 通用 AI 编程闭环；
- Prompt 模板；
- 基础设施与远程开发 Prompt；
- 权限与安全边界；
- 复杂任务拆分；
- 独立复核；
- 三个 AI CLI 的对照与协作。

---

## 当前项目阶段

主体写作已经完成，当前阶段是：

```text
全书统一重写
+ 重复内容压缩
+ 版本化事实核对
+ 附录和维护页同步
+ 公开前准备
```

不再以增加章节数量为主要目标。

---

## 近期优先级

### P0：完成内部稳定版

1. 完成全部正文的统一审查；
2. 更新术语表、危险命令清单和版本核对表；
3. 保持 README、SUMMARY、ROADMAP 和更新记录一致；
4. 检查 Claude Code、Codex CLI、Grok CLI、Docker、uv、Tailscale 和 PyTorch 的版本化内容；
5. 保持 Markdown 自动检查为 0 错误、0 警告。

### P1：人工质量复核

1. 检查跨章节重复；
2. 检查命令是否标明平台和运行位置；
3. 检查危险命令是否有影响范围、预览和恢复说明；
4. 检查外部链接和官方来源；
5. 用真实 Mac 与 Ubuntu 环境抽样验证关键命令；
6. 检查全文是否残留私人信息、真实 Token 或内部地址。

### P2：公开准备

1. 确定文档许可证；
2. 执行完整敏感信息扫描；
3. 准备公开版 `.gitignore`；
4. 新建干净公开仓库；
5. 只复制最终文件；
6. 创建单一首次发布提交；
7. 配置 README、Actions 和发布说明。

当前私有开发仓库不会直接切换为公开。

### P3：以后再考虑

- 文档站点；
- 全文搜索；
- 多版本文档；
- Release 自动化；
- 更多 AI CLI；
- Vim、Shell 脚本和系统服务的进阶内容。

---

## 完成标准

内部稳定版需要满足：

- 快速入口可以独立完成练习；
- `SUMMARY.md` 中全部本地链接存在；
- 正文没有截断、空章节或未闭合代码块；
- 版本化章节注明核对日期和官方来源；
- 危险操作附近有具体风险和停止方式；
- Mac 与 Ubuntu 的命令位置明确；
- 三个 AI CLI 的权限和登录路线不混淆；
- 自动检查为 0 错误、0 警告；
- 人工抽样验证通过；
- 仓库中不存在真实凭据和私人信息。

---

## 自动检查范围

当前自动检查覆盖：

- UTF-8；
- Markdown 一级标题；
- 围栏代码块闭合；
- 本地链接；
- 越界路径；
- `SUMMARY.md` 引用；
- 异常短文件和重复标题。

自动检查不能替代：

- 外部网址有效性；
- 技术事实核对；
- 软件版本兼容；
- 真实环境运行；
- 安全和隐私人工审查。

最新结果见 [自动检查报告](Appendix/自动检查报告.md)。