# 写作规范与内容审查标准

> V2.0 重写规范，最近更新：2026-07-29

这份文件不是为了把所有章节套进同一种模板，而是为了避免手册在持续扩写后变成一批互相割裂的命令卡片。V2.0 面向第一次系统学习终端和 AI 编程工具的读者，正文应当像有人坐在旁边解释操作过程：先交代现在处于什么场景，再展示操作，随后说明结果怎么看、出错时怎么办。准确性、安全边界和可复现性仍然排在最前面，但文字不能只剩下警告和结论。

---

## 1. 默认读者与写作目标

默认读者以 Mac 作为日常设备，对终端、Linux、Git 和配置文件的经验有限，可能同时使用 Claude Code、Codex CLI、Codex App、Grok CLI，以及 SSH 连接的 Ubuntu 机器。他们愿意理解原理，但不应被要求预先掌握系统管理、网络代理或软件配置知识。

新术语第一次出现时，要先用普通语言说明它解决什么问题，再给出准确名称。例如，不能只写“修改 Provider”，而应先说明：客户端需要知道请求发往哪个服务、使用哪个模型以及从哪里读取凭证，这组连接规则在 Codex 中称为模型供应商配置。术语一旦确定，后文保持稳定，不为了避免重复而不断换说法。

一本面向新手的教程至少要让读者做到三件事：知道命令应该在哪里运行，知道运行后要观察什么，也知道结果不符合预期时该停在哪里检查。只罗列正确命令不算完成教学。

---

## 2. 段落与版面

正文以完整自然段为主。通常一个段落由两到五句话组成，围绕同一个问题连续展开。同一件事情还没有解释完，不因为一句话结束就强制换段。

下面这种写法只适合速查表，不适合正文：

```text
创建文件：

<一个代码块>

创建目录：

<一个代码块>
```

教程正文应把相关操作组织成一个场景，例如先说明要在练习目录中创建文件和子目录，再集中展示命令，最后解释各行命令的效果。代码块前后的文字要承担解释任务，不能长期只使用“运行：”“查看：”“例如：”这类空引导。

小标题用于划分真正不同的问题。一节正文只有一两个短句时，应优先与相邻内容合并。横线不是段落分隔符，不必在每个小节结束后机械插入。列表只用于确实并列的选项、步骤或检查项；原因、背景和故障分析通常应写成连续段落。

---

## 3. 降低模板化和 AI 痕迹

V2.0 不追求每段都有一句漂亮总结。技术说明允许朴素，允许在必要位置重复关键词，也允许一个段落只完成一项普通解释。

以下句式并非绝对禁止，但整章中不能反复出现：

- “不是……而是……”；
- “本质上……”；
- “真正的……”；
- “需要注意的是……”；
- “不要把……理解成……”；
- “先……再……最后……”；
- “一句话总结……”；
- 为了收尾而制造的口号或金句。

生活化比喻只在抽象概念确实难懂时使用，而且比喻后必须回到技术定义。一个章节中出现一两个有效比喻即可，不要让 PATH、容器、分支、凭证和 Agent 全都拥有拟人化故事。俏皮话如果不能帮助读者判断操作结果，就删除。

避免大段使用整齐对仗的短句，也不要为了显得全面，给每个问题都补上完全相同的“风险、验证、恢复、结论”四段式。章节结构应由内容决定。

---

## 4. 新知识必须配套例子

新概念至少配一个能够运行或观察的例子。核心章节还应提供连续操作、错误恢复或对比例子。示例不只是命令本身，通常应包含以下信息中的大部分：

1. 读者现在处于什么目录、机器或程序界面；
2. 为什么此时需要执行这条命令；
3. 要输入的内容；
4. 一段具有代表性的输出；
5. 输出中应该重点看什么；
6. 常见错误是什么；
7. 怎样回到安全状态。

例如讲 `pwd` 时，除了给出命令，还应展示 `/Users/NAME/Projects/demo` 之类的输出，并说明这代表后续相对路径会从 `demo` 目录计算。讲 `cd` 时，应同时展示一次路径拼错后的 `no such file or directory`，再引导读者用 `pwd`、`ls` 和引号检查问题。

连续操作例子要贯穿一个真实小任务。文件操作章节可以从创建练习目录开始，依次创建、复制、移动和检查文件；Docker 章节可以从一个最小 Python 程序开始，完成构建、运行、挂载和数据验证。不要把互不相关的十条命令放在一起，然后称为完整案例。

---

## 5. 命令、输出与运行位置

命令代码块使用 `bash`，示例输出使用 `text`。默认不复制 Shell 提示符；必须展示提示符时，要明确说明提示符本身不要输入。

涉及多台机器或多层环境时，在代码块前直接标明运行位置，例如“在 Mac 终端执行”“在 Ubuntu 终端执行”“在容器内执行”“在 Claude Code 对话框输入”。同一节中频繁切换位置时，应在每个关键步骤重新标注，不能依赖读者记住几页前的上下文。

占位符使用醒目的大写形式，例如：

```text
PROJECT_PATH
SERVER_IP
MODEL_NAME
YOUR_USERNAME
```

示例输出必须看起来像真实输出，但不得包含作者的真实邮箱、内部地址、访问令牌或本机绝对路径。macOS 与 Ubuntu、BSD 与 GNU 工具、Apple Silicon 与 x86_64 的差异，需要在相关命令旁说明。

---

## 6. 错误示例与恢复路径

面向新手的章节不能只展示成功路径。以下类型的内容应主动加入错误示例：路径不存在、权限不足、命令未安装、配置文件语法错误、凭证无效、端点不兼容、端口已占用、Git 工作区不干净，以及客户端读取了错误配置层。

错误说明应回答三个问题：当前报错来自哪一层，下一步先检查什么，哪些操作暂时不要做。例如看到 HTTP 401 时，先检查凭证和请求头，不应立刻反复修改 Base URL；看到 404 或“model not found”时，应区分端点路径、API 协议和模型名称。

危险操作附近必须给出预览和恢复方式。仅写“谨慎使用”没有实际帮助。

---

## 7. 配置、凭证、供应商与实例

AI CLI 章节必须稳定区分以下对象：

```text
客户端：Claude Code、Codex CLI、Codex App 等
配置：模型、端点、权限和行为设置
凭证：OAuth 登录缓存、API Key、访问令牌
供应商：实际接收请求并提供模型的服务
配置管理工具：CC Switch、Cockpit Tools 等
实例：一套相互隔离的配置、凭证和运行状态
本地网关：位于客户端与上游服务之间的转发层
```

不能把“换模型”“换供应商”“换账号”“换 Profile”“启动第二个独立实例”写成同一件事。介绍第三方管理工具之前，先解释原生文件和环境变量；读者应该能够在不依赖 GUI 的情况下判断工具改动了什么，也能够恢复官方配置。

配置示例不得直接写入真实密钥。TOML、JSON 和 Shell 环境变量要分别解释语法，不能让新手复制一段配置却不知道哪部分是名称、哪部分是路径、哪部分会被当作秘密读取。

涉及第三方中转站、多账号切换、OAuth 复用或本地反向代理时，必须说明工具来源、数据流向、凭证接触面和服务条款风险。“能够运行”不能被写成“官方支持”或“没有封号风险”。

---

## 8. 凭证和隐私

不得在文档、截图、示例、测试输出或提交历史中保留 API Key、Refresh Token、Session Token、Cookie、SSH 私钥、真实邮箱、公网地址、Tailscale 认证链接、内部仓库地址、云服务凭据或真实训练数据。

`auth.json`、Keychain、Keyring、`.credentials.json` 等位置必须按密码级别对待。书中可以讲清楚它们承担什么职责，也可以教读者检查文件是否存在和权限是否合理，但不展示完整内容，不指导用户把浏览器会话或登录缓存交给来源不明的程序。

配置文件与凭证文件必须分别讨论。`config.toml` 或 `settings.json` 可能包含非敏感行为设置，也可能间接引用环境变量；`auth.json` 等文件则可能直接包含访问令牌。不能因为它们都在同一个目录，就建议整目录同步或上传。

---

## 9. 危险操作

出现下列内容时，必须紧邻说明影响范围、预览方法、恢复可能性和验证方式：

- `rm -rf`、`git reset --hard`、`git clean`、`git push --force`；
- `sudo`、修改系统服务、防火墙、SSH 或驱动；
- `rsync --delete`、卷删除、Docker Socket 和 `--privileged`；
- `curl | sh`、来源不明的安装脚本；
- 跳过 AI 审批或 Sandbox；
- 复制、替换、导入或批量切换认证凭证。

多账号和凭证管理章节尤其要避免把覆盖文件写成无害操作。任何替换前都应先说明备份位置、文件权限和恢复步骤。

---

## 10. 版本化事实与来源

Claude Code、Codex、Grok、CC Switch、Cockpit Tools、模型名称、API 协议和认证方式变化较快。相关章节应标注核对日期，优先使用官方文档、项目仓库和本机 `--help`，并明确区分官方功能与第三方扩展。

涉及当前版本的路径、配置键或命令时，不写成永久规则。章节中可以给出“截至核对日期”的用法，同时告诉读者怎样通过 `--version`、`--help`、`/status`、配置参考或发行说明自行确认。

第三方项目的 README 只能证明项目声称具备某项能力，不能证明上游平台认可该做法，也不能替代安全审计。

---

## 11. 目录、链接与重复内容

新增或重命名文件时，同步检查 `SUMMARY.md`、README、ROADMAP、前后章节链接和快速入口。内部链接使用相对路径。

共同原理只完整解释一次。例如配置层级、凭证保护和本地网关的概念集中放在配置与凭证部分；Claude Code 和 Codex 专章只补充各自实现差异。通过链接承接内容，不要在三个工具章节中复制同一段安全提醒。

---

## 12. 写入后的检查

每次修改后至少确认：一级标题存在，代码块闭合，本地链接有效，文件没有截断，没有重复二级标题，没有私人信息，没有未经说明的危险命令，自动检查为 0 错误、0 警告。

V2.0 还要增加人工文风检查：

- 是否连续出现多个只有一句话的正文段落；
- 是否出现大量“短引导 + 代码块”的碎片结构；
- 是否用列表代替了本应连续解释的内容；
- 是否每个新概念都只有命令，没有场景、输出和错误恢复；
- 是否反复使用相同的总结句式和比喻；
- 是否能从头连续读完，而不是只能当作命令索引。

自动检查只能发现结构问题。技术准确性需要官方来源核对，教学质量需要让没有相关经验的人按步骤试读。

---

## 13. 审查顺序

建议按以下顺序审查章节：

```text
事实和版本是否正确
→ 命令与配置是否可执行
→ 凭证和危险边界是否清楚
→ 新手能否理解当前场景
→ 例子是否包含输出与恢复
→ 段落是否连续、重点是否集中
→ 是否与其他章节重复
→ 链接和自动检查是否通过
```

内容准确和安全边界优先于文风，但准确不等于难读。V2.0 的目标是在不牺牲技术精度的前提下，让读者真正跟得上操作过程。