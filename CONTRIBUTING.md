# 写作规范与内容审查标准

> V3.0 叙事重构规范，最近更新：2026-07-30

本手册面向第一次系统学习终端和 AI 编程工具的读者。准确性、安全边界和可复现性仍然排在最前面，但正文不能因此退化成命令、警告和检查项的堆叠。V3.0 的重点是让每一章既能连续阅读，也能在实际操作时快速定位：读者应当知道自己处于什么场景、为什么执行某项操作、结果说明什么，以及出现异常时应该停在哪一层检查。

## 1. 默认读者与写作目标

默认读者日常使用 Mac，对终端、Linux、Git 和配置文件的经验有限，可能同时使用 Claude Code、Codex CLI、Codex App、Grok Build，以及通过 SSH 连接的 Ubuntu 机器。他们愿意理解原理，但不应被要求预先掌握系统管理、网络代理或软件配置知识。

新术语第一次出现时，先用普通语言说明它解决什么问题，再给出准确名称。例如，不能只写“修改 Provider”，而应先说明客户端需要知道请求发往哪个服务、使用哪个模型以及从哪里读取凭证，这组连接规则在 Codex 中称为模型供应商配置。术语确定后保持稳定，不为了避免重复而不断换说法。

一本面向新手的教程至少要让读者做到三件事：知道命令应该在哪里运行，知道运行后要观察什么，也知道结果不符合预期时该停在哪里检查。只罗列正确命令，或者只补充大量风险提醒，都不算完成教学。

## 2. V3.0 的叙事单位

自然段是完整意思的基本单位。通常一个段落由两到五句话组成，围绕同一个问题连续展开；同一件事情还没有解释完时，不因为一句话结束就强制换段。句数不是硬限制：危险警告、机器切换、关键结论和停止条件可以独立成段，但每个独立段落必须承担清楚的功能。

小节用于划分真正不同的问题，而不是表示“接下来运行另一条命令”。一节如果只有一句引导、一个代码块和一句解释，应优先与相邻内容合并。长章节可以有较多标题，但每节需要形成完整论述、操作阶段或故障判断，不能只是一张信息卡片。

章节应围绕一个连续任务、概念链或决策过程组织。不要先列出模板，再把“背景、命令、输出、风险、验证、恢复、总结”机械填满。不同内容需要不同结构：快速入门可以保留明确步骤，概念章节需要连续解释，故障章节应按因果和证据展开，工具对比则应围绕使用场景和选择标准组织。

## 3. 识别并消除碎片化

下面这种结构只适合速查表，不适合长期作为教程正文：

```text
创建文件：

<一个代码块>

查看结果：

<一个代码块>

注意安全。
```

教程应先说明当前场景和目标，再集中展示相关操作，最后解释各行命令与输出的意义。代码块前后的文字必须承担信息任务，不能反复只写“运行：”“查看：”“例如：”“可能看到：”“应看到：”。这些短引导如果没有独立价值，应与前后段落合并。

以下现象出现时，应暂停继续扩写并重构当前部分：

- 连续出现三个以上只有一句话的普通正文段落；
- 多个小节都只有少量正文和一个代码块；
- 页面很长，但主要由空行、标题、列表和代码块构成；
- 每一种模式、工具或参数都套用完全相同的介绍模板；
- 同一安全提醒在相邻章节重复完整出现；
- 删除过渡句后，章节之间仍然没有明确的因果或任务关系。

不能通过把多个短段粗暴拼成超长段落来解决问题。合并之后仍需检查段落是否围绕同一问题，句子之间是否有逻辑关系，读者是否能在需要时找到关键操作。

## 4. 列表、表格与代码块

列表只用于确实并列的选项、步骤、参数和检查项。原因、背景、原理和故障分析通常应写成连续段落；如果列表项必须依赖一整段解释才能理解，就应考虑改成正文。列表前要说明它解决什么问题，列表后要说明如何使用这些项目作出判断。

表格适合对比多个稳定维度，不适合承载长篇解释。不要为了压缩版面把整章改成表格，也不要在单元格中塞入复杂步骤和多段警告。表格中的结论如果存在重要前提，应在正文中解释。

命令代码块使用 `bash`，示例输出使用 `text`。默认不复制 Shell 提示符；必须展示提示符时，要明确说明提示符本身不要输入。代码块仍然单独展示，但叙事不应在代码块处断裂：前文交代运行位置、目的和影响范围，后文说明关键输出、成功标准和失败时的下一步。

## 5. 运行位置、输出与占位符

涉及多台机器或多层环境时，在代码块前直接标明运行位置，例如“在 Mac 终端执行”“在 Ubuntu 本机终端执行”“在容器内执行”“在 Claude Code 对话框输入”。同一节中频繁切换位置时，应在每个关键步骤重新标注，不能依赖读者记住几页前的上下文。

占位符使用醒目的大写形式：

```text
PROJECT_PATH
SERVER_IP
MODEL_NAME
YOUR_USERNAME
```

示例输出必须看起来像真实输出，但不得包含作者的真实邮箱、内部地址、访问令牌或本机绝对路径。macOS 与 Ubuntu、BSD 与 GNU 工具、Apple Silicon 与 x86_64 的差异，需要在相关命令旁说明。

输出示例不能只是装饰。正文应指出读者要观察哪一行、哪个字段或退出状态，以及输出不同意味着什么。若输出因版本而异，应说明稳定判断依据，而不是要求读者逐字匹配示例。

## 6. 连续案例与知识密度

新概念至少配一个能够运行或观察的例子。核心章节还应提供连续操作、错误恢复或对比例子。示例通常需要交代当前目录、机器或程序界面，执行目的，输入内容，代表性输出，观察重点，常见错误和安全恢复方式；这些信息不必全部拆成独立小节。

连续案例要贯穿一个真实小任务。文件操作章节可以从创建练习目录开始，依次创建、复制、移动和检查文件；Docker 章节可以从一个最小程序开始，完成构建、运行、挂载和数据验证。不要把互不相关的十条命令放在一起，然后称为完整案例。

内容密度提升主要来自删除重复、合并空泛过渡和重新组织结构，不来自省略必要解释。改写后应比较“单位阅读长度内获得的信息”是否增加，同时确认新手理解成本没有上升。

## 7. 降低模板化和 AI 痕迹

技术说明允许朴素，不追求每段都有漂亮总结，也不需要给每个问题制造一句口号。以下句式并非绝对禁止，但整章中不能反复出现：

- “不是……而是……”；
- “本质上……”；
- “真正的……”；
- “需要注意的是……”；
- “不要把……理解成……”；
- “先……再……最后……”；
- “一句话总结……”；
- 为了收尾而制造的金句。

生活化比喻只在抽象概念确实难懂时使用，而且比喻后必须回到技术定义。一个章节中出现一两个有效比喻即可，不要让 PATH、容器、分支、凭证和 Agent 全都拥有拟人化故事。

避免大段使用整齐对仗的短句，也不要给每个问题都补上相同的“风险、验证、恢复、结论”四段式。章节结构由内容决定。相邻段落如果反复重述同一判断，应保留解释最完整的一处，其余位置缩成必要提示或链接。

## 8. 错误示例与恢复路径

面向新手的章节不能只展示成功路径。路径不存在、权限不足、命令未安装、配置文件语法错误、凭证无效、端点不兼容、端口占用、Git 工作区不干净，以及客户端读取错误配置层等情况，应在相关章节提供可观察的错误示例。

错误说明需要回答三个问题：当前报错来自哪一层，下一步先检查什么，哪些操作暂时不要做。例如看到 HTTP 401 时，先检查凭证和请求头，不应立刻反复修改 Base URL；看到 404 或“model not found”时，应区分端点路径、API 协议和模型名称。

危险操作附近必须给出影响范围、预览方式、恢复可能性和验证方法。仅写“谨慎使用”没有实际帮助，但同一套通用警告不需要在每章完整复制；具体章节说明本次操作的独特风险，并链接到集中讲解位置。

## 9. 配置、凭证、供应商与实例

AI CLI 章节必须稳定区分以下对象：

```text
客户端：Claude Code、Codex CLI、Codex App 等
配置：模型、端点、权限和行为设置
凭证：OAuth 登录缓存、API Key、访问令牌
供应商：实际接收请求并提供模型的服务
配置管理工具：CC Switch、Cockpit Tools 等
实例：一套相互隔离的配置、凭证和运行状态
本地网关：位于客户端与上游服务之间的转发层
```

不能把“换模型”“换供应商”“换账号”“换 Profile”“启动第二个独立实例”写成同一件事。介绍第三方管理工具之前，先解释原生文件和环境变量；读者应能在不依赖 GUI 的情况下判断工具改动了什么，也能恢复官方配置。

配置示例不得直接写入真实密钥。TOML、JSON 和 Shell 环境变量要分别解释语法，不能让新手复制配置却不知道哪部分是名称、路径或秘密。涉及第三方中转站、多账号切换、OAuth 复用或本地反向代理时，必须说明工具来源、数据流向、凭证接触面和服务条款风险。“能够运行”不能写成“官方支持”或“没有封号风险”。

## 10. 凭证和隐私

不得在文档、截图、示例、测试输出或提交历史中保留 API Key、Refresh Token、Session Token、Cookie、SSH 私钥、真实邮箱、公网地址、Tailscale 认证链接、内部仓库地址、云服务凭据或真实训练数据。

`auth.json`、Keychain、Keyring、`.credentials.json` 等位置必须按密码级别对待。书中可以解释它们的职责，也可以教读者检查文件是否存在和权限是否合理，但不展示完整内容，不指导用户把浏览器会话或登录缓存交给来源不明的程序。

配置文件与凭证文件必须分别讨论。`config.toml` 或 `settings.json` 可能包含非敏感行为设置，也可能间接引用环境变量；`auth.json` 等文件则可能直接包含访问令牌。不能因为它们位于同一目录，就建议整目录同步或上传。

## 11. 危险操作

出现下列内容时，必须紧邻说明影响范围、预览方法、恢复可能性和验证方式：

- `rm -rf`、`git reset --hard`、`git clean`、`git push --force`；
- `sudo`、修改系统服务、防火墙、SSH 或驱动；
- `rsync --delete`、卷删除、Docker Socket 和 `--privileged`；
- `curl | sh`、来源不明的安装脚本；
- 跳过 AI 审批或 Sandbox；
- 复制、替换、导入或批量切换认证凭证。

多账号和凭证管理章节尤其不能把覆盖文件写成无害操作。任何替换前都要说明备份位置、文件权限和恢复步骤。危险提示应针对当前操作给出可执行办法，避免反复使用没有信息量的“请谨慎”。

## 12. 版本化事实与来源

Claude Code、Codex、Grok、CC Switch、Cockpit Tools、模型名称、API 协议和认证方式变化较快。相关章节应标注核对日期，优先使用官方文档、项目仓库和本机 `--help`，并明确区分官方功能与第三方扩展。

涉及当前版本的路径、配置键或命令时，不写成永久规则。章节可以给出截至核对日期的用法，同时告诉读者怎样通过 `--version`、`--help`、`/status`、配置参考或发行说明自行确认。第三方项目的 README 只能证明项目声称具备某项能力，不能证明上游平台认可该做法，也不能替代安全审计。

## 13. 目录、链接与跨章节去重

新增或重命名文件时，同步检查 `SUMMARY.md`、README、ROADMAP、前后章节链接和快速入口。内部链接使用相对路径。

共同原理只完整解释一次。例如配置层级、凭证保护和本地网关集中放在配置与凭证部分；Claude Code 和 Codex 专章只补充各自实现差异。确认机器和目录、Git diff、预演危险操作等原则应有明确主讲章节，其他位置只保留与当前任务直接相关的提醒。

跨章节删除重复前，要确认被删内容在主讲位置仍然容易找到。链接不是推卸解释：当前步骤需要读者立即知道的关键信息仍应简要说明，完整背景再交给链接承接。

## 14. 叙事结构审计

`scripts/audit_prose_structure.py` 用于统计普通正文中的微型段落、短段落、连续短段、薄小节和代码块前的空引导。脚本忽略代码块、标题、表格和纯列表，目的是定位热点，而不是自动评价文章好坏。

审计结果在 V3.0 第一阶段只作为报告，不设置全书硬性失败阈值。数字改善不能代替人工阅读，也不能成为粗暴合并段落的理由。样章和后续批次应同时比较基线、阅读连续性和内容覆盖情况。

运行全书审计：

```bash
python3 scripts/audit_prose_structure.py \
  --report prose-structure-report.md \
  --json prose-structure-report.json
```

比较当前工作区与主分支：

```bash
python3 scripts/audit_prose_structure.py \
  --baseline-ref main \
  --report prose-structure-comparison.md
```

## 15. 内容覆盖检查

深度重写不能只比较字数和段落数量。每章改写前后应逐项核对原内容，至少把重要信息归入以下状态之一：

```text
保留
合并表达
移动到其他章节
删除重复
因错误或过时而修正
```

独特命令、配置键、代表性输出、错误场景、安全边界和恢复方法如果消失，必须能够指出迁移位置或删除理由。没有覆盖说明的内容减少，默认视为需要复核，而不是自动视为精简成功。

## 16. 写入后的检查

每次修改后至少确认：一级标题存在，代码块闭合，本地链接有效，文件没有截断，没有重复二级标题，没有私人信息，没有未经说明的危险命令，自动检查为 0 错误、0 警告。

人工文风检查包括：

- 是否连续出现多个只有一句话的普通正文段落；
- 是否大量使用“短引导 + 代码块”的碎片结构；
- 是否用列表替代本应连续解释的内容；
- 是否出现只有一两个短句的小节；
- 是否反复使用相同总结句式和章节模板；
- 是否为了降低短段数量而制造难读的超长段落；
- 是否可以从头连续读完，而不只是当作命令索引；
- 是否与其他章节重复讲解共同原则。

自动检查只能发现部分结构问题。技术准确性需要官方来源核对，教学质量需要没有相关经验的读者按步骤试读。

## 17. 审查顺序

建议按以下顺序审查章节：

```text
事实和版本是否正确
→ 命令与配置是否可执行
→ 凭证和危险边界是否清楚
→ 原内容覆盖是否完整
→ 新手能否理解当前场景
→ 例子是否包含输出与恢复
→ 段落是否连续、重点是否集中
→ 是否与其他章节重复
→ 链接和自动检查是否通过
```

内容准确和安全边界优先于文风，但准确不等于难读。V3.0 的目标是在不牺牲技术精度的前提下，消除碎片化、模板化和无效篇幅，让读者真正跟得上操作过程。
