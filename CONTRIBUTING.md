# 写作规范与内容审查标准

> 最近更新：2026-07-28

这份文件用于保证《终端与 AI 编程手册》在持续修改后仍然准确、好读、可验证，并且不同章节不会逐渐变成互相重复的独立笔记。

---

## 1. 默认读者

默认读者：

- 以 Mac 为主力设备；
- 对终端和 Linux 经验有限；
- 使用 Claude Code、Codex CLI 或 Grok CLI；
- 可能通过 SSH 连接 Ubuntu GPU 机器；
- 希望理解原理，但不具备系统管理员背景。

重要术语第一次出现时，先用一句普通语言解释，再给准确名称。

例如：

> Shell 是负责理解并执行命令的程序。macOS 默认通常使用 zsh。

不要一上来只堆抽象术语。

---

## 2. 文风

### 准确优先

每一节应尽量回答实际问题：

- 这个东西是什么；
- 为什么现在需要它；
- 命令在哪里运行；
- 会读取或修改什么；
- 怎样确认成功；
- 失败后怎样停止或恢复。

### 可以轻松，但不能牺牲精度

允许少量生活化比喻和短玩笑，但后面必须跟准确解释。不要用热梗替代权限、路径、进程和版本边界。

### 删除口水话

避免：

- “这个真的非常重要”；
- “相信大家都知道”；
- “我们可以看到”；
- “总的来说要具体分析”；
- “接下来继续往下看”。

“重要”要说明影响；“复杂”要指出复杂的是哪一层。

### 减少模板化句式

不要每章重复：

- “核心结论”；
- “本质上”；
- “真正稳定的方式”；
- “推荐结构”；
- “需要特别警惕”；
- “一个工具负责……人负责……”。

相同原则只完整解释一次，其他章节通过简短提醒和链接承接。

### 不使用推荐指数

不再使用星级“推荐指数”。命令是否适合取决于场景、权限和风险，星级容易制造虚假的统一结论。

---

## 3. 章节结构

技术章节可以包含：

- 问题和使用场景；
- 心智模型；
- 最小可运行示例；
- 常见误解；
- 风险和恢复；
- 验证方法；
- 延伸阅读。

不要求机械套用固定模板。短文不必硬拆十个小节；小标题应帮助读者快速找到问题。

段落应像教程，不像聊天碎片。列表只用于真正并列、顺序明确或需要逐项检查的内容。

---

## 4. 命令示例

### 不复制提示符

写：

```bash
pwd
```

不要默认写：

```text
$ pwd
```

必须展示提示符时使用 `text` 代码块并明确说明不要复制。

### 明确运行位置

跨设备章节要标明：

```text
Mac 执行
Ubuntu 执行
容器内执行
AI CLI 中输入
```

### 占位符要明显

使用：

```text
YOUR_USERNAME
PROJECT_PATH
SERVER_IP
MODEL_NAME
```

不要把示例写得像所有人都能原样复制。

### 区分平台

macOS 与 Ubuntu 行为不同，或 GNU 与 BSD 工具参数不同，必须明确标注。Apple Silicon、Intel/Rosetta、x86_64 和 NVIDIA CUDA 也不能混写。

---

## 5. 危险操作

出现以下内容时，必须紧邻具体风险说明：

- `rm -rf`；
- `sudo`；
- `git reset --hard`；
- `git clean`；
- `git push --force`；
- `rsync --delete`；
- `curl | sh`；
- Docker Socket、`--privileged` 和数据卷删除；
- 修改 SSH、防火墙、驱动、系统服务或权限；
- 跳过 AI 审批或 Sandbox。

风险说明至少回答：

```text
会影响什么
怎样先预览
能否恢复
怎样验证
```

不能只写“谨慎使用”。

---

## 6. 凭据和隐私

不得在文档、截图、示例或提交中保留：

- API Key；
- Refresh Token、Session Token 或 Cookie；
- SSH 私钥；
- 真实邮箱、账号和公网地址；
- Tailscale 认证链接；
- 内部仓库和数据路径；
- 云服务凭据；
- 真实训练数据内容。

只写正规认证方式：供应商 API Key、官方 OAuth、企业身份或正式网关。不把浏览器会话数据包装成正常接入教程。

---

## 7. AI CLI 内容

必须区分：

```text
客户端
模型供应商
认证方式
模型
配置切换工具
```

例如 CC Switch 是配置管理工具，不是模型供应商；Claude Code 接入 DeepSeek 后，客户端仍是 Claude Code，但后端模型和数据处理方已经变化。

涉及 Agent 时，应默认要求：

- 先只读调查；
- 限定文件范围；
- 不读取凭据；
- 不执行 Git 提交和推送；
- 不安装依赖或修改系统配置；
- 完成后列出变更和验证证据。

---

## 8. 版本化事实

以下内容变化较快：

- Claude Code、Codex CLI、Grok CLI；
- 模型名称和 API 端点；
- Docker Desktop、Compose 和 uv；
- Tailscale；
- PyTorch、CUDA 和 NVIDIA Container Toolkit；
- 云平台认证方式。

相关章节应：

1. 标注核对日期；
2. 优先引用官方文档；
3. 提醒查看本机 `--help` 或版本；
4. 避免把当前版本号写成永久规则；
5. 不从旧论坛帖子复制固定安装命令。

---

## 9. 链接和目录

新增或重命名文件时必须同步检查：

- `SUMMARY.md`；
- README；
- ROADMAP；
- 上一篇和下一篇链接；
- 快速入口；
- 附录索引。

内部链接使用相对路径。不要链接到不存在的目录或未来计划文件。

---

## 10. 写入后的检查

每次修改后至少确认：

- 一级标题存在；
- 代码块闭合；
- 文件尾部没有截断；
- 本地链接有效；
- 没有重复标题；
- 没有私人信息；
- 没有未经说明的危险命令；
- 自动检查为 0 错误、0 警告。

自动检查通过不代表技术内容一定正确。版本化章节还需要官方来源核对，关键命令还需要真实环境抽样验证。

---

## 11. 提交信息

提交信息应说明修改目的，例如：

```text
润色：重写 SSH 故障排查
维护：同步目录与路线图
新增：Claude Code 接入 DeepSeek 专章
修复：补全被截断的代码块
```

避免：

```text
update
fix
changes
final
```

---

## 12. 审查顺序

推荐按以下顺序审查一篇章节：

```text
事实是否正确
→ 命令是否可执行
→ 风险是否说清楚
→ 结构是否容易查找
→ 是否与其他章节重复
→ 链接是否完整
→ 文风是否自然
```

内容准确和安全边界优先于排版统一。