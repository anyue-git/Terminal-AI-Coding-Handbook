# 更新记录

本文件记录《终端与 AI 编程手册》V2.0 的主要内容变化。详细提交历史以 Git 为准，当前审查结论见[内容审查记录](内容审查记录.md)。

## 2026-07-27：建立初始结构

- 创建 README、SUMMARY、ROADMAP 和 CONTRIBUTING；
- 建立 Part 01–12 与 Appendix 目录；
- 完成第一批终端、Git、SSH、Python、Docker、AI CLI 和 GPU 内容；
- 新增快速开始、快捷键速查、危险命令和术语表；
- 建立版本化工具核对表与内容审查记录。

## 2026-07-28：建立自动检查和第一轮事实审查

### AI CLI

- 核对 Claude Code、Codex CLI 与 Grok Build 的安装、权限、Sandbox、会话和非交互能力；
- 区分客户端、模型后端、供应商、认证方式和配置工具；
- 增加 Claude Code 接入 DeepSeek Anthropic 兼容接口与第三方供应商说明；
- 明确 CC Switch 是配置管理工具，不替供应商或中转商背书；
- 不把 Cookie、Session Token、共享认证缓存或“订阅转 API”作为正规接入方式；
- 建立三个 AI CLI 的对照协作和独立复核思路。

### Docker、Python 与 GPU

- 统一 Docker Desktop、Ubuntu Docker Engine、Compose、Volume、网络和权限边界；
- 强化 Docker Socket、特权容器、Bind Mount 和持久数据风险；
- 重写 Python 解释器、pip、venv、Conda、uv 和依赖复现内容；
- 删除容易过时的固定 PyTorch/CUDA 安装组合；
- 统一 NVIDIA Driver、CUDA Toolkit、Runtime、PyTorch 与 Container Toolkit 的分层；
- 建立 Mac 到 Ubuntu GPU 的端到端案例。

### SSH 与异网连接

- 重写局域网 OpenSSH、密钥、SSH Config、rsync、端口转发和故障排查；
- 强化主机指纹、`sshd -t`、第二终端验证和 Agent Forwarding 风险；
- 确立 Tailscale + 普通 OpenSSH 的异网主线；
- 不把公网 TCP 22 转发作为个人新手默认；
- 统一 tmux、日志、checkpoint 和结果同步边界。

### 自动检查

- 新增 `scripts/check_markdown.py`；
- 检查 UTF-8、本地链接、越界路径、代码块、一级标题、重复标题和异常短文件；
- 检查 Markdown 文件是否被 `SUMMARY.md` 引用；
- 新增 GitHub Actions 严格检查；
- 将警告纳入失败条件，避免维护债务积累。

## 2026-07-29：完成主体教程化重写

### 写作标准与快速入口

- 重写写作规范、README、ROADMAP 和 CONTRIBUTING；
- 重写终端十五分钟上手、AI CLI 快速上手和完整 Quickstart；
- 明确三个快速入口的不同职责；
- 减少碎片化段落、模板化句式和孤立命令卡片；
- 为关键操作增加运行位置、代表性输出、错误恢复和安全边界。

### Part 01–04：终端与 Git

- 重写 Terminal、Shell、zsh、路径和文件系统；
- 重写导航、文件操作、日志、搜索、管道、重定向和进程控制；
- 统一 zsh 快捷键解释，以本机 `bindkey` 为准；
- 重写 Git 心智模型、提交、分支、恢复和 Pull Request；
- 强化精确暂存、`git diff --cached`、reflog 和人工提交；
- 不把 `git add .`、hard reset、clean 和强制推送写成普通默认。

### Part 05–08：开发基础设施

- 重写 SSH 全部 4 篇；
- 重写 Homebrew 全部 2 篇；
- 重写 Python 环境全部 3 篇；
- 重写 Docker 全部 4 篇；
- 加入 Mac 与 Ubuntu、宿主机与容器、Volume 与 Bind Mount 的明确边界；
- 建立 Compose 多服务项目和 GPU 容器连续练习。

### Part 09：Claude Code

- 重写安装、登录、权限、Sandbox、会话、记忆、Hooks、MCP 和大项目工作流；
- 增加 DeepSeek、企业 Gateway 和第三方供应商接入专章；
- 区分客户端、认证、模型后端和数据处理方；
- 不默认使用 bypass permissions。

### Part 10：Codex CLI

- 重写安装、认证、Sandbox、审批、交互、自动化和 Git 协作；
- 区分 ChatGPT 登录、Platform API Key 和企业 Access Token；
- 区分稳定的 `sandbox_mode + approval_policy` 与 beta Permission Profiles；
- 增加 `codex exec`、JSONL、Schema、超时、恢复、分叉、Review 和 Worktree。

### Part 10B：Grok Build

- 重写安装、认证、Ask/Auto/Always-approve、Plan 和 Sandbox；
- 增加 Headless、会话、Worktree、Skills、Plugins、Agents、Hooks、MCP、LSP 与 ACP；
- 区分消费者订阅与 xAI API；
- 明确官方产品名 Grok Build，终端命令为 `grok`。

### Part 10C：配置、凭证与多实例

新增 4 篇核心章节：

1. 配置、凭证、供应商、模型与实例；
2. Codex TOML、Profile、auth.json、Keyring 与 `CODEX_HOME`；
3. Claude Code Settings、环境变量、认证优先级与 Gateway；
4. CC Switch、ccswitch 与 Cockpit Tools。

内容覆盖原生配置、第三方工具修改层、配置投影、凭证替换、本地路由、备份、观察、恢复和错误分类。

### Part 11：GPU 远程开发

重写全部 8 篇：

- Mac 与 Ubuntu 局域网部署；
- 项目同步与目录规范；
- tmux 与断线续跑；
- NVIDIA、CUDA 与 PyTorch；
- Mac 与 Ubuntu 分别创建环境；
- Tailscale 异网连接；
- VS Code、AI CLI 与 GPU 协作；
- 实验日志和 Checkpoint 管理。

默认场景使用 Mac 日常开发、Ubuntu 24.04 游戏本运行 NVIDIA GPU，并覆盖 DHCP 地址保留、Secure Boot/MOK、最小 CUDA 运算、短训练和 checkpoint 恢复。

### Part 12：AI 开发工作流

重写全部 7 篇，统一七阶段闭环：

```text
定义完成标准
→ 建立现实基线
→ 只读调查
→ 选择方案
→ 小批实施
→ 证据验证
→ 独立复核与人工提交
```

加入按阶段选择的 Prompt 模板、基础设施安全流程、权限分层、批次预算、证据账本、多 Agent Worktree 协作和完整 GPU 案例。

实验代码快照修正为：

```bash
git diff --binary HEAD > "$RUN_DIR/code.patch"
git ls-files --others --exclude-standard \
  > "$RUN_DIR/untracked-files.txt"
```

明确补丁不会自动包含未跟踪文件正文。

## 2026-07-30：附录与全书总复核

### 危险命令和安全边界

- 扩充递归删除、通配符、`find -delete`、覆盖、权限、Git 清理、远程脚本和强制推送；
- 增加 `uv sync` 精确同步可能移除额外包的提醒；
- 增加 `0.0.0.0` 网络监听、Jupyter/TensorBoard/本地网关暴露风险；
- 增加 Docker 数据清理、磁盘分区、进程批量终止和高权限远程 Agent；
- 增加 CC Switch、ccswitch、Cockpit Tools 的凭证与本地路由风险。

### 术语和版本化工具

- 重写术语表，补充 Provider、Base URL、Profile、实例、本地网关、精确同步、Run Directory、JSONL 等概念；
- 统一官方产品名 Grok Build，并说明兼容目录仍保留 Grok CLI；
- 更新 Claude Code、Codex、Grok Build、第三方配置工具、Docker、Python、GPU、SSH、Tailscale 和 GitHub Actions 核对项；
- 增加 Docker Compose v5 维护提醒；
- 删除对未确认稳定 Grok 子命令的永久承诺。

### 状态与审查记录

- 将旧的 70 个 Markdown 文件统计修正为 74；
- ROADMAP 从“第一阶段”更新为主体重写完成、进入发布前终审；
- 重写内容审查记录，区分自动检查结论与仍需实机验证的内容；
- 同步 README、SUMMARY 和 Draft PR 说明；
- 保持 V2.0 PR 为 Draft，不合并、不覆盖 V1.0 公共快照。

## 当前状态

```text
74 个 Markdown 文件
主体教程化重写完成
内部链接和 Markdown 严格检查通过
0 个错误，0 个警告
没有已知正文截断
没有显眼未完成章节
Draft PR 保持打开，尚未合并
```

下一阶段不再批量扩写，而是发布前人工终审：

- 在 Mac 与 Ubuntu 抽样执行关键命令；
- 检查外部官方链接；
- 扫描真实用户名、邮箱、地址、Token、私钥和内部信息；
- 全书通读，寻找重复警告、教学跳跃和术语不一致；
- 由作者决定许可证与公开范围；
- 从私有源仓库导出干净公共快照并创建版本发布。
