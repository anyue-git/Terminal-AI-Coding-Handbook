# 更新记录

## 2026-07-27：建立初始结构

- 创建 README、SUMMARY、ROADMAP 和 CONTRIBUTING；
- 建立 Part 01–12 与 Appendix 目录结构；
- 完成第一批终端、Git、SSH、Python、Docker、AI CLI 和 GPU 远程开发内容；
- 新增快速开始、快捷键速查、危险命令和术语表；
- 建立版本化工具核对表与内容审查记录。

## 2026-07-28：版本化事实与安全边界审查

### AI CLI

- 核对 Claude Code、Codex CLI 与 Grok CLI 的安装、权限、Sandbox、会话和非交互能力；
- 区分客户端、模型后端、供应商、认证方式和配置工具；
- 新增 Claude Code 接入 DeepSeek 官方 Anthropic 兼容接口与第三方供应商章节；
- 明确 CC Switch 是配置管理工具，不替模型供应商或中转商背书；
- 不把 Cookie、Session Token、共享认证缓存或“订阅转 API”作为正规接入方式；
- 新增三种 AI CLI 对照协作和双 Agent 复核模式。

### Docker、Python 与 GPU

- 统一 Docker Desktop、Ubuntu Docker Engine、Compose、Volume、网络与权限边界；
- 强化 Docker Socket、特权容器、Bind Mount 和持久数据风险；
- 重写 Python 解释器、pip、venv、Conda、uv 和依赖复现章节；
- 删除容易过时的固定 PyTorch/CUDA 安装组合，改用官方选择器与本机验证；
- 统一 NVIDIA 驱动、Toolkit、Runtime、PyTorch 与 Container Toolkit 的分层；
- 完成 Mac 到 Ubuntu GPU 的端到端案例。

### SSH 与异网连接

- 重写局域网 OpenSSH 部署、密钥、SSH Config、rsync、端口转发与故障排查；
- 强化主机指纹、`sshd -t`、第二终端验证和 Agent Forwarding 风险；
- 确立 Tailscale + 普通 OpenSSH 的异网主线；
- 不把公网 22 端口转发作为个人新手默认方案；
- 统一 tmux、日志、checkpoint 和结果同步边界。

### 自动检查

- 新增 `scripts/check_markdown.py`；
- 检查 UTF-8、本地链接、越界路径、代码块、一级标题、重复标题和异常短文件；
- 检查 Markdown 文件是否被 `SUMMARY.md` 引用；
- 新增 GitHub Actions 自动检查和报告刷新；
- 将警告也纳入最终严格检查，避免维护债务长期积累。

## 2026-07-28：全书系统重写

### 快速入口与项目说明

- 重写终端十五分钟上手、AI CLI 快速上手和完整 Quickstart；
- 重写 README、ROADMAP 和 CONTRIBUTING；
- 明确三个快速入口各自职责；
- 同步当前 70 个 Markdown 文件的真实状态；
- 保留私有开发仓库，未来通过干净新仓库发布公开版。

### 终端基础与命令

- 重写 Part 01、Part 02 和 Shell 快捷键章节；
- 统一 Terminal、Shell、zsh、路径和当前目录概念；
- 删除星级“推荐指数”；
- 压缩重复解释，保留最小示例、风险和排错；
- 明确 zsh 按键以 `bindkey` 为准；
- 统一文件操作、日志、搜索、管道、退出状态和进程控制。

### Git、SSH 与 Homebrew

- 重写 Git 心智模型、日常提交、恢复和 Pull Request 章节；
- 强化 `git diff --cached`、精确暂存和人工提交；
- 明确 `reset --hard`、`clean -fd` 和强制推送风险；
- 重写 SSH 四篇，按网络、端口、主机身份和认证分层排错；
- 重写 Homebrew 与 PATH、服务和多版本管理；
- 不推荐 `sudo brew`、全量无差别升级或递归修复权限。

### Python 与 Docker

- 重写 Python 环境三篇和 Docker 四篇；
- 明确虚拟环境、容器和虚拟机的区别；
- 明确 Mac 与 Ubuntu 分别创建环境；
- 统一 Compose、Context、Socket 和 GPU 容器安全边界；
- 删除版本号堆砌，改成稳定机制与当前验证命令。

### Claude Code、Codex CLI 与 Grok CLI

- 重写 Claude Code 前四篇，并新增供应商接入专章；
- 重写 Codex CLI 四篇；
- 重写 Grok CLI 三篇；
- 统一先调查、定范围、单阶段修改、测试、diff 和人工提交；
- 明确审批、Sandbox、Worktree、Hooks、MCP、Skills 与插件的不同边界。

### GPU 远程开发

- 重写 Part 11 全部八篇；
- 按用户实际设备条件组织 Mac + Ubuntu 24 游戏本方案；
- 统一局域网、Tailscale、rsync、tmux、CUDA、PyTorch、VS Code 和 AI CLI 协作；
- 建立唯一 run 目录、配置、日志、指标、环境、checkpoint 与完整性校验流程。

### 附录与维护页收尾

- 重写快捷键速查表；
- 扩充危险命令与高权限操作清单；
- 扩充终端、Git、SSH、Docker、AI Agent、模型供应商和 GPU 术语表；
- 重写版本化工具核对表，删除容易迅速过时的固定版本断言；
- 重写内容审查记录，加入最终 Pro 审查建议；
- 清理旧文件数量、已完成待办和早期阶段状态。

## 当前状态

主体写作与第一轮系统审查已经完成。当前交付标准为：

```text
70 个 Markdown 文件
内部链接和 Markdown 结构检查通过
快速入口、正文和附录状态一致
没有已知正文截断
没有显眼的未完成章节
```

自动检查结果以 [自动检查报告](自动检查报告.md) 为准。

下一阶段不是继续扩写，而是由高强度最终审查重点检查：

- 版本化事实是否在 2026-07-28 后发生变化；
- 外部官方链接是否有效；
- Mac 与 Ubuntu 上的关键命令是否可执行；
- 是否残留真实用户名、地址、Token 或其他隐私信息；
- 是否仍有跨章重复、教学跳跃或表述过度绝对；
- 公开版许可证、`.gitignore` 和仓库规则。